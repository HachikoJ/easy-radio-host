# Changelog

## 2026-09-09 · 点歌对话布局

- 对话历史独立滚动，输入区固定在面板底部；用户消息、小蓝回复和点歌结果采用清晰的层次与对齐，口播文稿以折叠区域保留。
- 输入框自动增高且有上限，发送按钮统一为 44 px 圆形控件，提供空白禁用与发送中反馈；失败保留草稿并在输入区提示。
- 适配明暗主题、手机横屏和软键盘可见区域，保持面板切换、中文输入法、多行输入与外部播放控制。
- 影响范围为点歌面板展示和输入反馈；接口、歌单与配置不变，回退本次提交可恢复旧布局。

## 2026-09-09 · 前端更新可见性与预览排版

- HTML、CSS 与 JavaScript 增加缓存重新校验，入口和应用模块引用带版本参数，避免浏览器混用旧样式与新页面；保留 ETag / 304，不强制刷新正在播放的标签页。
- 更新实际界面截图，中英文 README 统一为桌面主图、等宽手机预览与折叠补充画廊，保留导航、联系与赞赏信息。
- 影响范围为前端资源加载、文档与截图；回退本次提交并重启主服务可恢复原缓存策略，无需修改运行配置或数据。

## 2026-09-09 · 一屏收听与口播响度

- 首屏按视口高度分配唱片与歌词空间，歌词和按需面板独立滚动；歌曲标题与右对齐的跟随、动效、设置同处一行。开源致谢与作者 GitHub 移到右上角并在新标签页打开，致谢默认中文、点击后切换对应英文。
- 合并普通页与沉浸页，取消独立沉浸入口及第二套唱片渲染，常见屏幕统一为无需整页滚动的收听页；高倍率缩放下的极短视口保留滚动以保障操作可达。口播文稿在歌词区域展示，保留明暗外观和全部节目、收藏、偏好与点歌操作。
- MiniMax 默认音色改为 `Chinese (Mandarin)_Warm_Girl`；新生成的 MiniMax 与 edge-tts 口播使用 ffmpeg 两遍响度归一化，目标 -14 LUFS、真峰值上限 -1.5 dBTP，启用 `dual_mono`。缺少 ffmpeg、超时或处理失败时保留原音。
- 歌曲音量使用主音量的 0.85，口播使用主音量，减少切段音量落差；保留音量滑杆与静音行为，不改写第三方歌曲或既有口播。
- 影响范围为前端布局、资料页、播放音量与新口播生成；后端接口、歌单和本地记录格式保持兼容。回退本次代码与文档可恢复 `11bd954` 的行为；显式音色配置需从更新前环境备份恢复，已生成口播无需迁移。

## 2026-09-09 · 安静收听布局

- 首屏改为唱片与当前曲目配合大面积歌词；手机缩小曲目区，让更多空间留给歌词。主题电台、节目/收藏/历史与点歌互动按需展开，侧栏打开时底部播放器仍可操作。
- 跟随、动效和收听设置集中在歌词标题旁；连续播放、译文、歌词时差、选歌偏好及少推荐管理移入设置，保留偏好开启前的隐私告知。
- 歌词使用淡化主题照片背景与文字高亮，沉浸切换在支持的浏览器使用 View Transition，减少动态效果时直接切换。歌词入口聚焦现有歌词区并恢复跟随，不强制进入沉浸模式。
- 保留现有立体音频波幕、播放恢复、推荐和本地记录行为；同步中英文说明与界面截图。影响范围为前端布局、控件位置和视图过渡，后端接口、音源、音色配置及数据格式保持兼容。回退本次前端与文档提交即可恢复 `58afdf8` 的布局，无需数据迁移。

## 2026-09-09 · 立体霓虹波幕

- 保留中心主题图片和旋转唱片，外围径向线条替换为 Three.js 渲染的黑色圆环、彩色发光沿线与多层半透明立体波幕。64 个归一化对数频段驱动圆周局部起伏，能量、低频和层间延迟形成连续律动。
- 通过 `captureStream` 和 Web Audio 旁路分析音轨，保留原生音频输出；浏览器、CORS 或分析不可用时退为平滑播放动画。匿名 CORS 加载失败沿用既有一次重试并恢复普通加载，优先保障播放。
- WebGL 不可用时回退为 Canvas 2D 连续涟漪；动画上限 30 fps、像素密度上限 2，暂停、缓冲、隐藏页面、关闭动效或系统减少动态效果时停止。
- 随本地 Three.js 0.170.0 模块保留 MIT 版权与完整许可；波幕几何和 Shader 独立实现，不分发用户提供的参考图片。
- 更新对应中英文功能与版权说明。影响范围为前端动效和歌曲加载的 CORS 尝试；音源、代理接口、配置及歌单格式保持兼容。回退本次提交即可恢复 `fc4b1a4` 的唱片动效与加载行为，无需数据迁移。

## 2026-09-09 · 播放恢复与旋转唱片

- 播放地址解析增加媒体可用性检查，高码率地址失效时回退同一来源、同一歌曲 ID 的可用码率；修复 ID 中 `+` 等字符的查询编码。前端播放失败自动刷新一次，仍失败时保留重试和切换下一段。
- 歌词默认跟随播放；手动浏览后 3 秒恢复，普通聚焦不关闭跟随，显式关闭开关仍可持续浏览。
- 使用中心主题图片、旋转唱片和外围径向律动替换直条动效，暂停、缓冲与隐藏页面时停止，尊重动效开关和系统减少动态效果设置。动画独立实现，不是实时频谱；主题图片不冒充专辑封面。
- 更新中英文功能说明及网易云音乐等播放器通用唱片交互的参考说明，未复制其代码、品牌、皮肤或媒体资产。
- 影响范围为曲库代理的地址解析、前端失败恢复及歌词与动效展示；播放路径、用户配置及歌单格式保持兼容。所有码率不可用时返回 502，取代先前的 404；可用地址仍返回 307，支持可选 `refresh=1`。回退本次提交并重启 `tingjian` 与 `tingjian-musiclib` 可恢复 `1109dcf` 的行为，无需数据迁移。

## 2026-09-09 · 歌词与动效可见性

- 底部播放器持续展示当前歌词，新增直达歌词入口，打开后恢复歌词跟随。
- 扩大播放律动的可见区域，增加播放摘要、歌词区和底部动效；正确显示暂停、缓冲、关闭及系统减少动态效果状态。
- 区分正在获取、同步行数、未返回歌词、失败和超时，保留重试与时间调整。此更新不改变音源或歌词接口。

## 2026-09-09 · 推荐与偏好

- 自动节目融合主题、收藏、同歌手延伸和探索候选，程序选定歌曲后再生成口播；新增去重、近期过滤、歌手分散与真实推荐依据。
- 新增默认关闭的偏好选歌、可撤回的少推荐列表与同歌手延伸；仅开启时发送本地偏好标题，服务端不持久保存个人偏好，完整列表不进入主持模型。
- 小曲库、模型失败及同名歌曲身份均有回归覆盖；新增可复现离线推荐评测、桌面与手机截图、中英文说明和 Embeat 参考致谢。
- 自动节目选歌行为变化，主动点歌及播放接口保持兼容。回退本次独立提交可恢复 `9a45d36` 的自动选歌行为；无需数据库迁移，用户配置与歌单保持原样。

## Unreleased

- 将产品命名为「听间 · Tingjian」，保留主持人「小蓝」及现有仓库名称。
- 重做桌面与手机前端：六主题、节目单、固定播放器、明暗主题和沉浸模式。
- 增加本地收藏、实际播放后记录的收听历史、再次点播、键盘与系统媒体控制；再次点播按标题重新匹配歌曲，可能返回不同版本。
- 完善取消、重试、聊天草稿保护与插播状态处理；前端继续使用现有 `/api/show` 和 `/api/intent`，保留 `/api/chat` 兼容接口。
- 增加无密钥本地演示，使用本项目合成音频与固定回复；更新双语 README、真实界面截图、品牌资产与第三方版权致谢。
- 界面改造参考 Claudio 的交互，采用独立实现，并保留作者、来源版本和完整 MIT 文本；未改变整个仓库的授权状态。
- 恢复方式：需要恢复旧版时，可从提交 `a8db34a7452e4e0db50d794d06f3a6f7b5ef5f2f` 恢复 `backend/static/` 及对应文档资产；本次前端更新不变更后端配置与歌单格式。
- 统一项目文档，明确在线音乐 API 架构和部署方式。
- 增加项目协作、贡献、行为准则和安全说明。
