# Changelog

## 2026-09-09 · 歌词与动效可见性

- 底部播放器持续展示当前歌词，新增直达歌词入口，打开后恢复歌词跟随。
- 扩大播放律动的可见区域，增加播放摘要、歌词区和底部动效；正确显示暂停、缓冲、关闭及系统减少动态效果状态。
- 区分正在获取、同步行数、未返回歌词、失败和超时，保留重试与时间调整。此更新不改变音源或歌词接口。

## 2026-09-09 · 推荐与偏好

- 自动节目融合主题、收藏、同歌手延伸和探索候选，程序选定歌曲后再生成口播；新增去重、近期过滤、歌手分散与真实推荐依据。
- 新增默认关闭的偏好选歌、可撤回的少推荐列表与同歌手延伸；仅开启时发送本地偏好标题，服务端不持久保存个人偏好，完整列表不进入主持模型。
- 小曲库、模型失败及同名歌曲身份均有回归覆盖；新增可复现离线推荐评测、桌面与手机截图、中英文说明和 Embeat 参考致谢。
- 自动节目选歌行为变化，主动点歌及播放接口保持兼容。回退本次独立提交可恢复 `9a45d36` 的自动选歌行为；无需数据库迁移，用户配置与歌单保持原样。

## Unreleased

- 将产品命名为「听间 · Tingjian」，保留主持人「小蓝」及现有仓库名称。
- 重做桌面与手机前端：六主题、节目单、固定播放器、明暗主题和沉浸模式。
- 增加本地收藏、实际播放后记录的收听历史、再次点播、键盘与系统媒体控制；再次点播按标题重新匹配歌曲，可能返回不同版本。
- 完善取消、重试、聊天草稿保护与插播状态处理；前端继续使用现有 `/api/show` 和 `/api/intent`，保留 `/api/chat` 兼容接口。
- 增加无密钥本地演示，使用本项目合成音频与固定回复；更新双语 README、真实界面截图、品牌资产与第三方版权致谢。
- 界面改造参考 Claudio 的交互，采用独立实现，并保留作者、来源版本和完整 MIT 文本；未改变整个仓库的授权状态。
- 恢复方式：需要恢复旧版时，可从提交 `a8db34a7452e4e0db50d794d06f3a6f7b5ef5f2f` 恢复 `backend/static/` 及对应文档资产；本次前端更新不变更后端配置与歌单格式。
- 统一项目文档，明确在线音乐 API 架构和部署方式。
- 增加项目协作、贡献、行为准则和安全说明。
