# 项目协作约定

## 项目目标

easy-radio-host 是一个基于 FastAPI 的在线 AI 音乐电台：使用 DeepSeek 编排节目，优先使用阿里云百炼 Qwen-TTS，并可按 MiniMax、edge-tts 降级合成口播，通过在线音乐 API 动态获取歌曲播放地址。项目不保存歌曲音频文件。

## 变更边界

- 优先保持现有 Python、FastAPI、systemd 和在线音乐代理架构，不为小需求引入新的框架或抽象。
- 修改前先确认实际调用关系和配置来源；保护已有用户配置、歌单和未提交改动。
- 影响公共 API、播放链路、默认配置、权限或数据格式的改动，必须在提交说明中写清影响范围和恢复方式。
- 不提交 `radio.env`、API 密钥、Cookie、运行时 `data/`、TTS 音频和临时备份文件。
- 歌曲来源、播放地址和版权说明必须保持真实；不要把在线 API 描述成项目自有音源。

## 文档要求

- README 面向使用者，说明当前在线能力、部署方式、API 和歌单维护方法。
- 部署文档只保留当前可复现的步骤，不记录个人机器、一次性排障过程或已经废弃的方案。
- 配置变量名称即使因兼容性保留 `NAS_*`，文档也要说明它们指向在线曲库代理。

## 验证要求

提交前至少执行：

```bash
git diff --check
python3 -m py_compile backend/app.py musiclib/proxy_server.py musiclib/resolve_multi.py
```

涉及播放、API 或部署配置时，补充对应的接口或配置验证，并在提交说明中区分通过项、既有失败和环境限制。

## GitHub 工作流

- 提交信息使用简短、可检索的动词开头，例如 `docs:`、`fix:`、`feat:`。
- 提交前检查 `git diff`、敏感信息和工作区状态；不要覆盖或回滚无关的用户改动。
- 只有在用户明确要求时才执行 `commit`、`push`、创建 PR、发布版本或公开部署。
- 推送前确认目标仓库和分支；推送后核对远端提交 SHA，并报告验证结果。
- 公开发布或完整质量审计时，补充真实截图、双语 README、许可证和适用的项目质量检查；普通维护改动不扩大范围。
