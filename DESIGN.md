# 听间设计约定

- 产品名：听间 / Tingjian；AI 主持人保留小蓝。产品名称不构成商标可用性的法律结论。
- 受众：希望少做选择、按主题连续听歌的个人听众。
- 方向：轻量音乐播放器。主色为冷白与石墨灰，蓝色用于播放和选择，珊瑚红用于提醒。复用已设计的对话声波标记。
- 层次：主题封面与节目操作优先，六个主题、节目单、对话分别成区；桌面固定侧边导航，手机顶部导航；播放条始终在底部。
- 字体：系统中文无衬线；标题 20/26/30 px，正文 12/14 px，说明 10/11 px；不使用视口比例字号。
- 几何：内容区不套浮动卡片，重复主题项使用 6 px 圆角，控件尺寸固定；照片保持原比例裁切。
- 状态：空节目、编排中/取消、播放/暂停、插播、完成、网络失败/重试、输入保留；减少动效偏好关闭旋转反馈。
- 接口：沿用 `/api/show` 与 `/api/intent`。所有服务返回的文本用 `textContent` 渲染。
- 资产：主题摄影是氛围封面，不标为具体歌曲封面；使用本地 Lucide 图标。演示音频是本地合成原创音序，不冒充在线歌曲。
- 验证：375/768/1024/1440 px，检查溢出、图片加载、键盘操作、进度/音量、插播恢复、失败与取消。

## Claudio 参考迭代

产品分支：个人音乐播放器，以当前曲目为视觉中心；沿用听间品牌。主参考是 Claudio 的播放器/侧栏交互，冷白与石墨灰为本项目配色，未复制上游的发光和背景皮肤。来源、适配与后续数据条件记录在 [CLAUDIO-ADAPTATION.md](docs/CLAUDIO-ADAPTATION.md)。

五条视觉原则：当前曲目优先；主题图明确身份；主内容不嵌套卡片；明暗模式保持文本对比；手机保留稳定的播放控制。

五条交互原则：模式切换不打断音频；收藏可以直接取消；历史只由真实播放产生；再次点播重新取链且保护输入草稿；系统/键盘控制复用一个播放状态机。

普通页保留主题、节目/收藏/历史页签与聊天；沉浸页采用无框摄影和曲目/口播布局，可随时退出。小屏改为上下排列，不把桌面控件缩小。沉浸视图不伪造歌词或音频频谱。

收藏/历史仅保存此浏览器的标题、主题与时间，演示数据隔离。读写失败可在内存继续，损坏条目跳过。文稿、标题和历史均使用文本 API，媒体元数据不包含密钥或临时直链。
