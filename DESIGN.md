# 听间设计约定

- 产品名：听间 / Tingjian；AI 主持人保留小蓝。产品名称不构成商标可用性的法律结论。
- 受众：希望少做选择、按主题连续听歌的个人听众。
- 方向：轻量音乐播放器。主色为冷白与石墨灰，蓝色用于播放和选择，珊瑚红用于提醒。复用已设计的对话声波标记。
- 层次：首屏只展开收听主体验，以视口高度扣除顶栏与播放器后的空间分配唱片和歌词，歌词与辅助面板独立滚动。歌曲标题与右对齐的跟随、动效和设置同处一行，手机用紧凑唱片区让出歌词空间。顶部导航按需展开主题电台、节目/收藏/历史及点歌侧栏；播放条始终在底部且不受侧栏遮挡。
- 字体：系统中文无衬线；歌曲标题 17/20/22 px，歌词 19/21/24 px，正文 12/14 px，说明 10/11 px；不使用视口比例字号。
- 几何：内容区不套浮动卡片，重复主题项使用 6 px 圆角，控件尺寸固定；照片保持原比例裁切。
- 状态：空节目、编排中/取消、播放/暂停、插播、完成、网络失败/重试、输入保留；减少动效偏好关闭旋转反馈。
- 接口：沿用 `/api/show` 与 `/api/intent`。所有服务返回的文本用 `textContent` 渲染。
- 资产：主题摄影是氛围封面，不标为具体歌曲封面；使用本地 Lucide 图标。演示音频是本地合成原创音序，不冒充在线歌曲。
- 验证：375/768/1024/1440 px，检查溢出、图片加载、键盘操作、进度/音量、插播恢复、失败与取消。

## Claudio 参考迭代

产品分支：个人音乐播放器，以当前曲目为视觉中心；沿用听间品牌。主参考是 Claudio 的播放器/侧栏交互，冷白与石墨灰为本项目配色，未复制上游的发光和背景皮肤。来源、适配与后续数据条件记录在 [CLAUDIO-ADAPTATION.md](docs/CLAUDIO-ADAPTATION.md)。

五条视觉原则：当前曲目优先；主题图明确身份；主内容不嵌套卡片；明暗模式保持文本对比；手机保留稳定的播放控制。

五条交互原则：外观切换不打断音频；收藏可以直接取消；历史只由真实播放产生；再次点播重新取链且保护输入草稿；系统/键盘控制复用一个播放状态机。

使用单一收听页面与一套唱片渲染，合并原普通页和沉浸页，取消独立沉浸入口。页面按视口高度布局，歌词与辅助面板在各自区域滚动，整个收听页不滚动。主题、节目/收藏/历史页签与聊天通过按需侧栏访问；侧栏打开时底部播放器仍可操作，支持 Escape、关闭按钮及返回入口，关闭后恢复触发控件焦点。唱片中心使用既有主题摄影，不标为实际专辑封面；小屏缩小唱片区，歌词占满下一行，不缩小播放控件。主持人讲话时在歌词区展示口播文稿，推荐依据与节目操作仍可从对应位置访问，不伪造歌词或音频频谱。

收藏/历史仅保存此浏览器的标题、主题与时间，演示数据隔离。读写失败可在内存继续，损坏条目跳过。文稿、标题和历史均使用文本 API，媒体元数据不包含密钥或临时直链。

常见桌面与手机视口保持一屏。为保障高倍率缩放下的操作可达性，视口高度不超过 320 CSS 像素时允许整页滚动，避免把歌词和操作压缩到零高度。

## 选歌偏好与推荐依据

歌曲标题所在行的右侧放置「跟随」「动效」与收听设置图标。收听设置以弹出区域集中连续播放、可用译文、歌词时差、选歌偏好和少推荐管理，支持外部点击与 Escape 关闭；弹出区域按可用视口高度限制高度并内部滚动，不遮住底部播放器。默认关闭偏好选歌，开启前展示本地歌曲标题的发送目的、范围与关闭方式；完整收藏和历史不进入主持模型提示词。演示与正式偏好隔离。

- 曲目旁的 Lucide Radio、ThumbsDown 图标分别对应同歌手延伸与少推荐，提供明确名称、悬停提示和 44 px 触控范围；少推荐可以逐首撤回，从下期生效，保留主动点歌。
- 当前曲目和节目单显示服务端实际推荐依据。近期曲目回补、歌手不足等降级说明独立展示，不宣称声学相似度或喜欢概率。
- 同歌手延伸、生成新一期均复用节目加载、取消与错误恢复；失败保留原节目和偏好。普通跳过、网络错误不记为不喜欢，最近播放只在真实 playing 事件更新。
- 桌面与手机保留相同操作；歌曲标题与跟随、动效和设置同处一行，收藏、同歌手延伸和少推荐放在唱片下方的操作区。少推荐列表使用单层行布局，恢复后焦点移至下一项或列表标题。

## 歌词与播放动效

参考 Qiaomu Music Player Web（`a8519a23e0038a49cfa9495cc0b420a3b4e4825d`）及常见播放器的通用交互，按听间现有页面独立实现，不复制参考项目源码、视觉皮肤或媒体资产。具体来源和 lrc-kit 1.2.1 的 MIT 许可及修改范围见 [第三方来源与致谢](THIRD_PARTY_NOTICES.md)。

- 歌词通过现有 GD 音乐 API 代理获取，按来源及歌曲 ID 匹配；显示版权与来源。只在内存缓存最多 128 条、每条 300 秒，不写入磁盘或随仓库再分发歌词库。
- 仅有 LRC 时间戳时高亮当前行与自动跟随；纯文本保持静态，不用歌曲总时长猜测歌词时间。解析使用随项目保留许可的 lrc-kit。
- 歌词默认跟随播放；点击有时间戳的行跳转至对应位置，允许 0 秒。手动浏览暂缓自动居中，停止操作 3 秒后恢复；仅显式关闭跟随开关才持续停止，普通聚焦不关闭跟随。歌词延后数值限制为 -10 至 +10 秒，有时间戳的译文可选显示。
- 空结果与网络失败提供重试；口播与歌曲歌词状态区分，换曲不残留上一首歌词。演示使用原创器乐，歌词区域明确标注原创演示文案、非歌曲原词。
- 主效果使用本地 Three.js 0.170.0 模块：旋转唱片中央放置主题图，外围以实际黑色环形几何承托彩色发光沿线和多层半透明立体波幕。64 个归一化对数频段映射圆周局部高度，整体能量与低频控制冲击，层间延迟形成延绵起伏。WebGL 不可用时回退为 Canvas 2D 连续涟漪。
- 绘制上限 30 fps、像素密度上限 2；等待、暂停、结束和隐藏页面时停止，记住用户开关，尊重 `prefers-reduced-motion`。参考图仅用于理解空间造型，几何和 Shader 独立实现，不将用户上传参考图加入项目资产。
- 支持 `captureStream` 的浏览器在歌曲匿名 CORS 播放后捕获音轨，通过 Web Audio `AnalyserNode` 旁路分析。分析分支不连接 `destination`，不使用 `createMediaElementSource`，保留原生音频输出。不支持捕获、CORS 或分析不可用时，自动使用随播放时间变化的平滑涟漪，不宣称所有设备都提供真实音频频谱。
- 歌词区默认可见，底部常驻当前歌词，点按底部歌词聚焦现有歌词区并恢复跟随，不强制切换模式。歌词区采用低透明度的主题照片背景和文字层次高亮，活动行不加蓝色背景条；行间滚动平滑并尊重减少动态效果偏好。加载、无歌词、失败和超时保留明确提示，同步行数供辅助技术读取，不能以空白让用户猜测是否检索成功。
- 旋转唱片外围的连续立体波幕取代径向线条，中心主题图片与外围律动保持稳定尺寸。借鉴网易云音乐等播放器的通用唱片交互，几何造型、样式和动画独立实现，不复制品牌、代码、皮肤或专辑图片；Three.js 的 MIT 版权与完整许可随本地模块保留。跟随和动效开关本身体现启停状态，系统减少动态效果仍优先于动效开关。
- 验证覆盖桌面与手机的视口高度适配、明暗模式、长歌词换行、手动滚动、0 秒跳播、时间偏移、可选译文、口播文稿、无歌词/失败与重试，以及暂停/缓冲/后台/减少动态效果时的动效停止。

## 播放失败恢复

- 推荐及点歌在入队前通过录音身份匹配和实际音频 Range 校验；失效候选依次尝试其他码率、匹配录音与渠道，保留歌名、歌手及版本约束。结构化查询编码保留 ID 中的 `+` 等字符。
- 播放失败先自动刷新原地址一次，再检索未失败的匹配音源。确认无可用匹配后展示具体曲名提示再继续下一段；限流、超时或未检索完时保留重试，不误报无音源。停止、暂停或切曲取消恢复，迟到响应不能覆盖新曲目；连续失败不无限生成新节目。口播文字降级保持兼容。
- 网页歌曲通过本站流转发并支持 Range，音频分析与播放使用一致的同源请求。已验证的新音源复用缓存，避免立即刷新再次消耗额度；歌词使用对应 `lyric_id`。
- 恢复不修改用户歌单、配置或歌曲身份。回退本次代码提交并重启服务即可恢复先前行为，无需数据迁移。

## 点歌对话

- 点歌面板内的消息独立滚动，输入区固定在面板底部，保留外部播放器；用户消息右对齐，小蓝回复左对齐，点歌结果以音乐图标和曲名区别。空白状态采用短问候，不添加功能介绍或示例按钮。
- 输入框使用 16 px 字体，随内容增加高度并设上限，发送按钮保持 44 px 圆形触控范围。空白与发送中禁用提交，发送中显示加载图标；失败在输入区显示错误并保留草稿，成功后仅清空仍与已发送内容相同的文本。中文输入法组合输入与 Shift+Enter 保持原行为。
- 当前口播文稿保留在消息区的可展开区域。消息最多保留 40 条，切换面板保留历史与草稿。颜色复用明暗主题，焦点边界显示在整个输入区。
- 依据实际可见视口而非仅页面高度适配软键盘；短屏启用紧凑输入，极窄的可见高度允许面板向顶部扩展，关闭按钮与发送按钮保持可达。

## 声音与响度

- 主持人口播优先使用阿里云百炼 `qwen3-tts-instruct-flash` 的 `Cherry` 音色，按 `Qwen -> MiniMax -> edge-tts` 降级；`QWEN_TTS_VOICE`、`MINIMAX_VOICE` 和 `EDGE_TTS_VOICE` 可显式覆盖。声音相关配置变化只影响新生成的口播，不重写已有音频；固定播报和串场文件也按声音版本命名，避免升级后混用旧音色。
- Qwen、MiniMax 与 edge-tts 新口播使用 ffmpeg 两遍 `loudnorm`，目标 -14 LUFS、真峰值上限 -1.5 dBTP、LRA 7，按 `dual_mono` 测量单声道的双扬声器播放响度；每遍超时 20 秒。缺少 ffmpeg、分析值无效、超时或处理失败均保留原音，不能以响度处理失败中断已生成口播。
- 原生播放器主音量统一由滑杆控制，歌曲应用 0.85 系数、口播应用 1.0 系数；切段时更新系数，静音状态保持一致。第三方歌曲不下载、不重新编码，不声称固定系数可以统一所有曲目的实际响度。

## 资料入口

- 开源致谢与作者 GitHub 使用右上角图标入口，保留可访问名称与悬停提示，均在新标签页打开，避免中断收听；外链设置 `noopener noreferrer`。
- 致谢默认只展示中文，点击 English 后切换完整对应英文；切换保留展开状态，许可证原文、来源版本与版权边界始终可查。README 同样默认中文、英文单独点击切换。
